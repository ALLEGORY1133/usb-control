services:
  - type: web
    name: usb-control
    runtime: python
    region: oregon
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: python manage.py migrate --noinput && python manage.py createsuperuser --noinput --skip-checks 2>/dev/null || true && gunicorn core.wsgi:application
    plan: free
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: DATABASE_URL
        fromDatabase:
          name: usb-control-db
          property: connectionString

databases:
  - name: usb-control-db
    plan: free
    region: oregon
